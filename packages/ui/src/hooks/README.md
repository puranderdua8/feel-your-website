# Custom hooks for @feel-your-website/ui.

#

# shadcn components that ship a hook (e.g. `use-mobile` for `sidebar`) write

# it here. Kept as a real directory so `components.json`'s `hooks` alias

# resolves before the first such component lands.
